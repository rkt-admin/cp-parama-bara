<script context="module">
  export const prerender = true

  export const load = async ({ fetch }) => {
    return {
      props: {
        recentPosts: await fetch('/posts.json?page=1&limit=2').then((res) => res.json())
      }
    }
  }
</script>

<script>
  import PostPreview from '$lib/components/PostPreview.svelte'
  import HeroComponent from '../../components/Hero.svelte'

  export let recentPosts
</script>

<div class="mx-auto flex flex-col flex-grow w-full max-w-4xl">
  <HeroComponent />
  <div class="mt-6">
    {#each recentPosts as post}
      <div class="flex p-4 mb-2 border-b">
        <PostPreview {post} small />
      </div>
    {/each}
  </div>
</div>